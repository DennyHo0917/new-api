# API Route 自建后端开发计划与任务清单 (TodoList)

## 2026-09-22 管理员系统设置多列布局

- [x] 将管理员系统设置页改为响应式多列布局：宽屏两列分区且每个分区内两列字段，窄屏自动回落单列；选择框、输入框与保存按钮紧凑排列，原有逐项保存逻辑不变。前端生产构建通过（2026-09-22）。

## 2026-09-21 SubRouter 价格同步频率

- [x] 将 SubRouter 上游价格同步由每日调整为每 10 分钟，同时保留 BaseLLM 官方价格每日同步与启动时立即同步；定向价格同步测试与根模块构建通过（2026-09-21）。

## 2026-09-21 忘记密码业务结果与 Turnstile

- [x] 忘记密码仅在后端明确接受请求后显示“已发送”；分站公开配置提供 Turnstile 开关与站点公钥，前端提交有效 token，并区分业务拒绝与网络失败；不存在邮箱继续返回一致的接受结果。覆盖 Turnstile 关闭、开启后缺少/无效/有效 token，以及前端 `success:false`/缺失结果回归；middleware/controller 定向测试、后端构建、前端认证检查与生产构建通过，本地 `5173` 已连接真实 VPS（生产当前 Turnstile 关闭）。依据 OWASP ASVS 5.0.0 V6.3 与 Forgot Password、Authentication、Session Management Cheat Sheets（2026-09-21）。

## 2026-09-21 调用日志经营数据

- [x] 管理后台调用日志新增消费渠道、用户消费、成本、收益四列；渠道优先展示真实渠道名，金额统一按当前币种显示，成本按现行 5% 上游加价反推，收益为用户消费减成本。补齐四语言表头，日志回归检查与前端生产构建通过（2026-09-21）。

## 2026-09-21 未选分组严格最低价

- [x] 将历史空分组密钥与新建“全站调用”密钥统一为严格最低价路由；自动路由只允许名称包含 `standard` 的分组，`enterprise` 必须由用户明确选择；最低价 standard 分组不可用或请求失败时只在该分组内重试，不得自动切换到更贵分组。回归覆盖旧空分组密钥、enterprise 自动过滤、最低价固定重试、最低价不可用时拒绝跨组及显式 enterprise 密钥；service/middleware 全包与 controller 定向测试通过，真实 VPS 定价确认 `kimi-k3` 的自动候选仅为 `kimi standard=0.071803`（2026-09-21）。

## 2026-09-21 全站调用最低价路由

- [x] API Keys 页面已隐藏容易混淆的 `default` 固定分组选项，仅保留一个“全站调用”入口；`auto` 密钥会按请求模型和用户分组倍率动态优先最低有效价格分组，渠道亲和不会绕过最低价分组。该初版跨组回退行为随后由“未选分组严格最低价”规则进一步收紧。服务与中间件测试、前端表单检查和生产构建通过；本地 `5173` 页面及代理到真实 VPS 的 69 模型定价接口验证通过（2026-09-21）。

## 2026-09-21 本地前端真实后端联调

- [x] 固化本地前端使用 `5173` 并通过 `origin.api-route.com` 连接真实 VPS 后端的规则；VPS 只读确认 Nginx 按域名转发到健康的后端容器，修复开发代理误拦截 `/api-keys` 及未加载 `.env.local` 的问题。页面、站点接口和含 69 个模型的定价接口本地实测通过，前端生产构建通过（2026-09-21）。

## 2026-09-21 API Keys 分组选择器

- [x] 新建密钥选择分组时复用模型广场的分组 Logo；名称包含 `standard` 的分组使用绿色粗体，包含 `enterprise` 的分组使用橙色粗体。定向 Logo 映射检查与前端生产构建通过（2026-09-21）。

## 2026-09-21 管理后台链路性能优化

- [x] 将 `www.api-route.com/api/*` 通过 Cloudflare Worker 直接回源 `origin.api-route.com`，绕过 Vercel API Rewrite；请求方法、查询参数、请求体、Cookie 与响应原样透传回归通过，生产响应已无 `x-vercel-*`。已真实删除 Worker 验证请求自动回落至原 Vercel Rewrite，再部署版本 `f63898b3-c703-4d1c-8cff-7282e97d7ce2` 恢复直回源（2026-09-21）。
- [x] 管理日志页拆分核心列表与辅助统计/系统信息加载，避免最慢的辅助请求阻塞列表展示；回归检查与生产构建通过（2026-09-21）。
- [x] 管理后台登录恢复时不再预拉普通用户日志；用户日志页仍保留按需预取和缓存，回归检查与生产构建通过（2026-09-21）。

## 2026-09-21 全局功能审计修复

- [x] 2026-09-21 恢复 AI API Reseller 页面顶部导航入口：对照只读旧前端确认 `/ai-api-reseller-platform` 页面与多语言路由仍存在，入口不再受已关闭的 `allow_sub_dist` 购买开关影响；桌面顶部导航和移动菜单共用该入口。前端生产构建及四语言 Reseller 预渲染检查通过。
- [x] 2026-09-21 丰富 AI API Reseller 介绍页：参考只读旧前端恢复适用对象、运营模式、平台能力、职责分工、对比与常见问题；删除自动部署、支付流程和旧站截图相关内容，完成四语言页面与 SEO 文案。前端生产构建、四语言预渲染检查和禁用文案扫描通过，本地预览已启动供验收；尚未推送。
- [x] 2026-09-21 修正 AI API Reseller 四语言 SEO 标题重复品牌名：中文恢复旧站的搜索意图标题，其余语言保留明确的 Reseller 标题，由 SEO 渲染器统一追加站点品牌；生产构建、四语言预渲染检查及生成标题核验通过。
- [x] 2026-09-21 管理后台改为全宽布局：移除桌面端居中最大宽度与左右大块留白，左侧菜单贴齐视口左边，右侧内容使用全部可用宽度，同时保留移动端必要边距；前端生产构建与差异检查通过。
- [x] 2026-09-21 调整后台用户列表字段：新增实际生效返佣比例与注册时间列，删除请求数列；返佣比例由后端按用户显式覆盖或邀请人数阶梯统一计算。模型列表回归、根模块构建与前端生产构建通过。
- [x] 2026-09-21 修复旧站 Key 余额耗尽后的无感接管：保留原 Key，不要求用户更换；收到 SubRouter 明确额度耗尽后，将迁移 Key 转为继承新站用户分组，使后续充值余额可直接使用；已写入 `subrouter_exhausted` 的历史标记会在首次请求时自动修复。完整鉴权、Redis 缓存命中、缓存失效失败回滚回归及根模块构建均通过。
- [x] 2026-09-21 完善后台用户管理：增加用户级返佣比例调整，留空继续按邀请人数阶梯计算，显式值按 0%–100% 覆盖并用于后续充值返佣；新增已消耗金额列，余额与已消耗金额支持后端全量升降序；操作按钮改为带悬停说明和无障碍标签的小图标。定向 Go 回归与前端生产构建通过；PostgreSQL 15.19 已验证全新建表、保留既有用户升级和重复迁移，既有数据不受影响。全量 Go 测试仅有两项 Windows HTTP/2 连接测试首次受本机连接中止影响失败，单独重跑均通过。
- [x] 修复邀请链接与邀请码留存：邀请中心和首次调用成功卡片生成的链接统一指向主页 `/?aff=...`；应用启动时校验 1–64 位字母、数字、下划线或连字符的邀请码并保存到兼容旧生产流程的 `localStorage.dist_aff`，后续邮箱注册与 OAuth 注册继续携带该邀请码。与只读生产前端 `/register?aff=...` 实现完成对照，行为回归与前端生产构建通过（2026-09-21）。
- [x] 修复旧站余额快照永久冻结：已有迁移用户每次成功密码登录后都会重新读取 SubRouter 当前余额并更新仅展示的 `legacy_quota`，不再因 `legacy_quota_updated_at > 0` 永久跳过；刷新失败保留上次成功快照，本地可消费额度始终不增加。SQLite service 全量回归及 PostgreSQL 15 隔离库定向回归通过，临时库与角色已删除（2026-09-21）。
- [x] 修复分站 API Key 高级设置保存：创建、更新和列表接口已正确接收、返回并持久化模型白名单、启用状态与 IP 白名单；移除前端无后端支持的路由优先级等伪设置。controller 定向测试与前端表单检查通过（2026-09-21）。
- [x] 修复 SubRouter 历史 Key 明确耗尽后直接更新数据库却未失效 Redis Token 缓存的问题：切换前先发布缓存失效与 fence，缓存失效失败则不改数据库；覆盖 Redis 命中后的下一请求本地路由及 Redis 故障时保持原分组/旧余额的回归，middleware/model 测试通过（2026-09-21）。
- [x] 下线已停用的套餐购买功能：删除可编辑前端 Packages 页面、导航、页面专用 API 与回流逻辑，历史套餐链接统一重定向至充值页；保留后台既有数据与后端接口。前端脚本检查及生产构建通过（2026-09-21）。

## 2026-09-20 切流前运行安全加固

- [x] 保留并展示旧站余额：用户通过旧站凭据登录时读取 SubRouter 当前余额，作为独立 `legacy_quota` 快照保存；前端展示余额为本地余额与旧站余额之和，但本地数据库可消费 `quota` 不增加，历史 Key 继续路由 SubRouter，明确额度耗尽后原子清零旧余额标记并切换对应历史 Key。已有迁移用户再次使用密码登录时会尽力刷新旧站余额，失败不阻断本地登录且不记录密码。覆盖设置字段保留、异常额度拒绝且不落库、普通限流不清零、明确耗尽不改本地额度等回归；service/middleware/controller/model 全量测试、后端构建及 VPS PostgreSQL 15 隔离库实测通过，临时库与角色已删除（2026-09-21）。
- [x] 用户头像统一展示：Google、GitHub、X、Discord、OIDC、Telegram、Linux DO 及自定义 OAuth 在提供头像时保存公开 HTTPS 地址，既有第三方用户下次登录或绑定时自动补齐；右上角优先显示第三方头像，地址缺失或加载失败时回退到显示名称、用户名或邮箱首字母。拒绝非 HTTPS、含凭据或超过 2048 字符的头像地址，头像保存失败不阻断登录。OAuth/model/controller 全量测试、后端构建与前端生产构建通过（2026-09-20）。
- [x] 修复旧站密码校验成功后 API Key 同步失败：回源的用户信息和 Key 请求带上旧站要求的 `New-Api-User` 身份头，不放宽密码校验或本地账户防覆盖规则。回归测试会拒绝缺失/错误身份头的 Key 同步；service 定向及全量测试、controller 编译和 `go build ./...` 通过（2026-09-20）。
- [x] 修复真实旧站登录响应未必携带用户 ID 时的迁移失败：登录成功后复用认证 Session 查询旧站 `/api/dist/user/self` 或 `/api/user/self` 补全用户信息；预同步占位记录在上游仍不返回 ID 时使用已核对的 `subrouter_id`，新用户则仍必须获得有效 ID。回源失败日志仅记录 `upstream_auth_failed` / `not_eligible` / `token_sync_failed` 等分类，不记录密码或 Session。已覆盖嵌套/平铺登录响应、用户信息回退、占位 ID 回退、Key 失败回滚及本地账户防覆盖；SQLite、PostgreSQL 15.19 隔离库、service 全量回归与 `go build ./...` 通过，临时库和角色已删除（2026-09-20）。
- [x] 根治切流后旧站用户首次登录：新站尚无本地记录时，仅在 SubRouter 凭据验证成功、返回有效旧站用户 ID 且历史 Key 抓取成功后，事务性创建 Quota=0 的本地账户并导入 Key；失败不留半迁移账户，已有本地账户不得被回源覆盖，预同步占位还会校验旧站 ID 一致。已覆盖错误凭据、Key 抓取失败回滚、重复迁移与本地账户防覆盖；SQLite 定向与 service 全量回归、PostgreSQL 15.19 隔离库实库回归、controller 编译及 `go build ./...` 通过，临时库与角色已删除。保留统一登录失败响应与现有限流，依据 OWASP ASVS 5.0.0 V6/V7 及 Authentication / Session Management Cheat Sheets。后端 `75d300e42` 已推送并部署，生产容器 healthy、重启数 0（2026-09-20）。
- [x] 修复 Google 登录用户管理与默认 API Key：后台用户列表同时展示用户名、Google 显示名称、邮箱和 Google 唯一 ID；分站创建 Key 未显式指定额度模式时默认跟随用户余额，仍保留显式限额能力，避免新用户自动生成的 Key 固定为 0 额度并返回 401。OAuth 唯一标识仅在管理员接口展示，不写日志；`google_18` 的既有默认 Key 已修正为跟随用户余额，生产 `/v1/models` 返回 200，调用接口在本地余额为 0 时正确返回 403“用户额度不足”而非 401“Invalid token”。model/controller 回归与前端生产构建通过，后端 `829059739`（Deploy #53 成功）与前端 `65af2d5` 已部署（2026-09-20）
- [x] 已有密码账号与 Google 同邮箱时不再只报“邮箱已占用”：OAuth 回调返回不含邮箱的绑定引导码，用户须先登录原账号并以密码、TOTP 或 Passkey 完成敏感操作验证，再发起与当前会话绑定、10 分钟过期、单次消费且使用 PKCE S256 的 Google 绑定；绑定成功后可直接使用 Google 登录，不复制余额、密钥或创建第二个用户。覆盖同邮箱不泄露、会话绑定及 PKCE 回归，controller/router 全量测试、后端构建、四语言文案检查与前端生产构建通过。依据 OWASP ASVS 5.0.0 V7.5.1/V7.6.2、Authentication / Session Management / OAuth2 Cheat Sheets（2026-09-20）。
- [x] 切流后修复旧站密码用户 `tt818900@163.com` 无法登录：旧站分销数据确认用户 ID 19480 且账号启用，生产 PostgreSQL 确认本地记录缺失；完整备份后仅写入单条密码留空、Quota=0、`subrouter_id=19480` 的迁移占位记录，未复制旧站余额（2026-09-20）
- [x] 用户通过支付宝、Stripe 或链上支付首次充值成功后，向企业微信机器人推送用户名、入账金额和充值渠道；机器人地址仅由 VPS 环境变量提供，重复支付回调不重复通知，通知失败不回滚用户入账；后端 `22166c66c` 已推送部署，生产容器健康且企业微信实测返回 `errcode=0`（2026-09-20）
- [x] 修复 BaseLLM 元数据将 `hy3` 发布为全零占位价、导致模型广场长期显示 ¥0.0000 的问题；每日同步现固定采用腾讯 TokenHub 公开价（输入 $0.15/M、缓存读取 $0.0375/M、输出 $0.60/M），并增加上游再次返回零价时仍保持非零定价的回归覆盖；后端 `980189257` 已推送部署，生产定价接口已验证为非零（2026-09-20）
- [x] 仅在 SubRouter 明确返回旧额度耗尽语义时标记历史 Key 已耗尽，普通限流 `429`、通用 `402` 及其他非额度错误保持原响应且不得切换到本地额度（现有代理与双轨网关测试通过，提交 `4abd11da4` 已推送并部署，2026-09-20）
- [x] 为生产后端、PostgreSQL 15 与 Redis 容器配置健康检查，并验证健康状态不改变现有数据、端口和服务行为（三个容器均为 healthy，用户数据与公开接口复核通过，2026-09-20）
- [x] 清理 VPS 无用 Docker build cache，保留运行中容器、镜像与数据卷，并复核磁盘空间和服务健康（释放 42.2 GB，根分区使用率由 86% 降至 19%，2026-09-20）

## 2026-09-18 用户与账户页面接入

- [x] 接入密码找回/重置与登录二次验证页面，保持恢复响应不可枚举、验证码限时且单次使用（前端构建与认证会话检查通过）
- [x] 接入个人资料、语言偏好、登录会话、Passkey、TOTP、第三方绑定、个人访问令牌和账户注销（敏感操作沿用后端安全证明与会话轮换）
- [x] 接入 API Playground 与模型定价详情页（Playground 复用现有实时请求工作台）
- [x] 删除模型排行榜与用户签到的后端路由、服务、数据模型、设置项及内置前端入口（保留已有数据库表，避免破坏性迁移）
- [x] 完成前后端回归测试、构建验证和本地提交（前端 `3a9c9f6`；前后端构建通过，前端认证检查通过；Go 全量测试存在既有共享数据库/用例状态失败）

## 2026-09-18 邮箱验证注册与密码登录

- [x] 恢复前端邮箱/密码登录与注册入口，保留第三方 OAuth 登录（注册页新增邮箱验证码发送与倒计时）
- [x] 分站注册强制邮箱验证码，第三方 OAuth 免验证；验证码限时、成功注册后失效，并对发送与登录/注册限流
- [x] 后端通过 Resend API 发送验证码，不向浏览器暴露邮件服务密钥（VPS 使用 `RESEND_API_KEY`，并保留 HTTP 邮件网关与 SMTP 回退）
- [x] 补齐回归测试并完成前后端构建验证（Go 定向测试、`go build ./...`、前端 `bun run build` 与认证检查通过）
- [x] 封堵通用 `/api/user/register` 绕过：所有密码注册入口无条件要求邮箱验证码，第三方 OAuth 注册不受影响；生产全局邮箱验证已启用，未上线阶段产生的 15 个零余额脚本账号已备份并清理。验证码继续保持限时、单次使用并受发送/注册限流保护；依据 OWASP ASVS v5.0.0-6.3.8、Authentication 与 Email Validation and Verification Cheat Sheets（2026-09-19）

## 2026-09-17 前端接管清理与管理闭环

- [x] 修复 GPT 型号名称包含 `spark` 时被误判为讯飞的问题，并让模型广场分组 Logo 始终优先采用分组厂商；生产已确认 `gpt-5.3-codex-spark` 归属 OpenAI（后端 `8f2457d99`，前端 `e662905`，2026-09-19）
- [x] 修复后台分组重命名只改倍率键、未迁移渠道模型归属的问题；重命名现在会原子迁移渠道、模型分组、能力、密钥、用户、订阅和相关配置，生产 `deepseek pro` 已安全重命名为 `deepseek enterprise`，六个模型归属保留（后端 `a0411a4df` 已推送，前端 `fb7585f` 已本地提交，2026-09-19）
- [x] 生图、生视频模型按 SubRouter 上游原生计费单位加价 5%，不再套用渠道分组折扣或展示 OFF；生产已验证 `gpt-image-2` 售价 $0.01605882 / ¥0.1076 每次，高于上游约 ¥0.1040，媒体渠道有效倍率均为 1（后端 `a199a13e0`、`d6d6e1372` 已推送部署，前端 `91852a4` 已本地提交，2026-09-19）
- [x] 补齐 Nano Banana 上游别名的官方价格同步，并修复每日官方价格同步使用接口字段名写入存储层而导致整批失败的问题；模型广场已删除分组倍率提示与卡片底部协议名，补齐 Nano Banana 的 Google 厂商与彩色 Logo；生产接口已验证 `nano-banana-pro`、`nano-banana-2` 均返回官方表达式价格（后端 `53baf2e29`、`6b523fab7` 已推送部署，前端 `1ed9175` 已本地提交，2026-09-19）
- [x] 将模型倍率从全局模型维度迁移为“实际渠道 + 模型”维度：未指定分组的密钥允许跨分组路由，指定分组时严格限制；预扣、重试与结算均按最终成功渠道倍率执行，模型广场仅显示可达渠道最低价；已完成失败回退覆盖、旧配置迁移，并恢复重命名后的 `deepseek pro` 六个模型与分组 Logo（后端 `df34fd9c3`、`40c3a8ca8` 已推送，前端 `01358fb`、`7c06b0d` 已本地提交，2026-09-19）
- [x] 后端每日同步当前渠道全部模型的 BaseLLM 官方原生厂商价格，峰谷模型固定采用官方峰值价；模型广场按“官方价 × 分组倍率”展示，上下文阶梯价格按最高档展示，并补齐 DeepSeek `-0731` / `-0813` 版本别名映射（定向 Go 回归、全量 Go 构建、前端价格解析检查及生产构建通过，2026-09-19）
- [x] 定价页接入后端模型广场数据，并移除模型列表静态兜底；已按 New API 模型广场改为全宽左侧固定筛选、右侧彩色厂商 Logo 模型卡片，并补充分组价格对比、价格详情、模型简介/标签/厂商/端点展示；模型广场不展示内部 `default` 分组，分组列表仅显示名称；模型卡片按当前分组价与官方基础价显示 `XX% OFF`；官方平坦表达式显示具体输入/输出/缓存价格，未配置基础模型价格时明确提示，不再展示 37.5 兜底倍率计算出的假价格；中文模型广场按站点每日 USD/CNY 汇率显示人民币，其他页面与实际计费仍使用美元（前端构建及浏览器验证通过）
- [x] 模型广场渠道分组改为按“文本与推理模型 / 图像生成模型 / 视频生成模型”折叠收纳，点击分类后展开具体分组，“全部模型”保持顶层入口；补齐四语言文案与分类回归检查（前端 `8cf46d8` 已本地提交，2026-09-20）
- [x] 模型广场与模型详情按页面语言显示厂商名：中文页面保留中文厂商名，英文、日文、韩文统一采用官方英文品牌名，并覆盖默认中文厂商映射及筛选逻辑（前端 `2f0c166` 已本地提交，2026-09-20）
- [x] 后台分组管理支持调整模型广场分组显示顺序，顺序持久化到后端并由公开模型广场接口返回；新增分组自动补到末尾，删除分组自动忽略残留顺序项（2026-09-19）
- [x] 后台“模型”列表支持为单个模型设置倍率；模型倍率存在时覆盖渠道分组倍率，留空时使用渠道分组倍率，模型广场展示与实际计费采用同一优先级（2026-09-19）
- [x] 基于旧站公开价格按约 5% 上浮校准 SubRouter 标准渠道：创建并去重 `gpt`、`claude`、`gemini`、`deepseek`、`glm`、`kimi`、`qwen`、`grok`、`seedace`、`gpt-image`、`nano banana` standard 渠道；统一继承既有 SubRouter 渠道密钥，为偏离分组目标的模型配置独立倍率，并为 11 个渠道的全部 55 个模型建立实际路由分组能力记录（生产 PostgreSQL 与运行时接口验证通过，2026-09-19）
- [x] 修复 11 个 standard 渠道复制了源渠道完整 `model_groups`、导致后台分组模型数为 0 而模型广场仍按能力表显示的问题；已备份非密钥配置，按各渠道实际模型重建归属，并验证渠道配置与运行时能力表的 14 个分组计数完全一致（2026-09-20）
- [x] 删除前端邮箱绑定、任务中心、发票、KOL 高级返佣、Creem、应用市场（相关入口/API、死代码和无引用资源已移除；登录入口已收敛为第三方 OAuth）
- [x] 简化分站介绍页为介绍与联系我们邮件入口
- [x] 管理界面补齐密钥分组、模型与分组倍率控制入口（分组倍率、模型元数据新增、渠道配置；普通管理员可读写分组倍率和模型定价）
- [x] 实现后端返佣提现记录与前端提现闭环（事务扣减、提现记录、并发回归测试；SQLite 迁移通过，MySQL 5.7/PostgreSQL 9.6 用例已接入 CI，待实际运行）

## 一、 项目背景与目标

### 1.1 业务现状与核心痛点
* **前端资产**：拥有现成的前端分销站点 `api-route-deploy`（基于 Vite + React 18 开发），已部署在 Vercel 并绑定正式域名 `www.api-route.com`。
* **上游受制**：此前前端的 API 请求全部反向代理到第三方平台 SubRouter（`https://apiroute.subrouter.ai`）。平台不开放后端源码与数据库权限，客户资源、交易流水和 API Key 均受制于上游。
* **垫资痛点（核心商业防线）**：
  * 老用户之前充值的款项已被 SubRouter 收取，存量余额在旧站。
  * **绝不能在新站自行垫资垫付旧余额**（否则将由站长个人资金承担大模型调用成本）。
  * 必须建立**双轨透明网关**：老用户的旧 API Key 流量透明透传给 SubRouter 扣除旧余额（站长成本为 0）；待旧余额耗尽或用户发起新充值时，资金进入站长自有收款通道，请求无缝切入新站独立后端。

### 1.2 核心目标
1. **建立完全自主可控的 New API 后端**，支持 SQLite、MySQL、PostgreSQL 三大数据库。
2. **实现接口契约完全兼容**：后端提供全套 `/api/dist/*` 路由规范，使现有的 `api-route-deploy` 前端无需重构业务代码即可无缝切换。
3. **基于 TxHash 验证的加密货币充值能力**：支持 **Arbitrum One (USDT/USDC)** 与 **TRON (TRC20 USDT)** 等链，用户转账后提供交易哈希（TxHash），后端链上查询确认，**严格以钱包实际入账的代币金额为准**自动换算 Quota 上账。
4. **实现双轨智能路由网关**：根据 API Key 自动识别并分流（本地新 Key 走本地渠道，未迁移旧 Key 透传 SubRouter 扣旧额度），实现“零垫资、无感过渡”。

---

## 二、 详细开发任务清单 (TodoList)

> **执行规范**：依据 `AGENTS.md` 约束，每完成一项任务，必须立即将对应条目的 `[ ]` 勾选为 `[x]` 并注明完成记录。

### 模块一：数据库模型与系统配置扩展 (Database & Config)
- [x] **1.1 加密货币充值订单数据模型 (`CryptoTransaction`)** *(已完成并通过自动化测试)*
  - [x] 设计模型字段：
    - `id`: 主键
    - `trade_no`: 内部唯一订单号（如 `CRYPTO-20260916-XXXX`）
    - `user_id`: 充值用户 ID
    - `chain`: 链标识（`arb` / `tron` 等）
    - `token`: 代币标识（`USDT` / `USDC`）
    - `wallet_address`: 平台收款钱包地址
    - `expected_amount`: 用户预估充值金额
    - `actual_amount`: **链上实际入账金额（以此为准结算 Quota）**
    - `quota_amount`: 实际到账折算的配额
    - `tx_hash`: **用户提交的链上交易哈希（全局唯一，防重放核心）**
    - `from_address`: 付款方钱包地址（链上解析存证）
    - `status`: 订单状态（`pending`: 待提交哈希/等待确认, `processing`: 链上确认中, `success`: 充值成功, `failed`: 校验失败, `expired`: 超时关闭）
    - `fail_reason`: 失败原因（如非目标地址、合约不符、交易失败等）
    - `expired_at`: 订单过期截止时间
    - `created_at`, `updated_at`
  - [x] 索引与唯一约束设计：
    - `trade_no` 唯一索引
    - `tx_hash` 唯一约束（防止同一笔转账哈希被重复提交/多次上账）
    - `user_id + status` 用户订单索引
  - [x] 适配 GORM 自动迁移，确保 SQLite、MySQL (>=5.7.8)、PostgreSQL (>=9.6) 三库完全兼容 (已注册至 `model/main.go` AutoMigrate 并通过单元测试验证)
- [x] **1.2 套餐与订阅数据模型设计** *(已完成并注册迁移)*
  - [x] 设计 `Package` 套餐模型（名称、原价、现价、周期、包含额度、状态等，见 `model/package.go`）
  - [x] 设计 `UserPackageSubscription` 订阅记录模型（用户ID、套餐ID、起止时间、状态）
  - [x] 确保多数据库迁移幂等与兼容性 (已注册至 `model/main.go` AutoMigrate)
- [x] **1.3 系统运营配置项扩展 (`setting/operation_setting`)** *(已完成)*
  - [x] 新增支持的加密链与钱包地址配置：
    - `ArbWallet`: Arbitrum One 收款地址（EVM 0x...）
    - `TronWallet`: TRON (TRC20) 收款地址（波场 T...）
    - 各链官方 USDT / USDC 合约地址白名单
  - [x] 新增链上 RPC 节点 / API 配置（如 Arbitrum 公开 RPC / Infura / Alchemy，TronGrid API Key）
  - [x] 新增最小充值金额与订单有效期配置（默认 30 分钟）
  - [x] 新增站点公告、汇率（USD/CNY）、品牌展示配置存储与动态读取 (见 `setting/operation_setting/crypto_setting.go`)

---

### 模块二：基于 TxHash 链上验证与实际金额入账引擎 (Crypto Engine via TxHash)
- [x] **2.1 订单创建接口 (`POST /api/dist/topup/crypto/pay`)** *(已在 `controller/crypto.go` 完成；已补充 NaN/Inf/正数/系统上限校验，Arbitrum 与 TRON 收款地址已配置为默认公开地址，仍可由环境变量覆盖)*
  - [x] 鉴权校验（解析登录态，获取当前用户 ID）
  - [x] 入参校验：充值金额 `amount`、选择的链 `chain`（`arb` / `tron`）、代币 `token`（`USDT` / `USDC`）
  - [x] 生成订单入库（初始状态 `pending`），返回平台对应的收款钱包地址、订单号 `trade_no` 及有效时间
  - [x] 加密货币最低充值默认值调整为 $5
  - [x] 支付窗口固定为前 10 分钟、TxHash 补交窗口为后 5 分钟；订单满 15 分钟后由后端拒绝继续核验（后端 `7a43918bf`）
- [x] **2.2 用户提交交易哈希接口 (`POST /api/dist/topup/crypto/submit`)** *(已在 `controller/crypto.go` 完成)*
  - [x] 入参：`trade_no`（订单号）、`tx_hash`（用户转账后的交易哈希）
  - [x] 防重校验：检查数据库中该 `tx_hash` 是否已被使用，防止重放攻击
  - [x] 绑定 `tx_hash` 到订单，将状态置为 `processing`，触发链上实时核验
- [x] **2.3 链上多链交易核验与实际金额读取服务** *(已在 `service/crypto_service.go` 完成并通过自动化测试)*
  - [x] **TRON (TRC20 USDT) 链上验证器**：
    - [x] 调用 TronGrid / 波场节点接口查询该 `tx_hash` 交易详情及合约调用事件
    - [x] 严格校验交易状态（`contractRet == "SUCCESS"` 且已完成打包确认）
    - [x] 严格比对目标合约地址为官方 USDT 合约（`TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t`）
    - [x] 严格比对接收地址为平台的 `TronWallet`
    - [x] **提取交易实际转移的代币数量（精度 6 位），得到 `actual_amount`**
  - [x] **Arbitrum One (USDT/USDC) 链上验证器**：
    - [x] 调用 Arbitrum RPC（`eth_getTransactionReceipt`）查询交易回执
    - [x] 校验 `status == "0x1"`（执行成功）
    - [x] 解析 Transfer 日志事件（`Topic[0] == Transfer`）：
      - 校验合约地址为官方 Arb USDT (`0xFd086bC7CD5C481DCC9C85ebE478A1C0b69FCbb9`) 或 USDC (`0xaf88d065e77c8cC2239327C5EDb3A432268e5831`)
      - 校验接收方地址为平台的 `ArbWallet`
      - **从 Data 提取实际转入代币数额（精度 6 位），得到 `actual_amount`**
  - [ ] **交易超时与失败容错**：若链上暂未确认，保持 `processing` 由定时任务继续重试（2026-09-17 核查发现尚未实现：当前查询错误直接标记 failed，未找到自动重试任务）。
- [x] **2.4 实际入账结算与 Quota 上账事务** *(已在 `model/crypto_transaction.go` + `service/crypto_service.go` 完成)*
  - [x] 开启数据库事务：
    - [x] 校验 `actual_amount > 0`
    - [x] 根据 `actual_amount` 严格计算用户应得 Quota（1 USDT = 1 USD Quota = 500,000 配额，使用 `common.WalletQuotaFromDecimalStrict` 杜绝溢出）
    - [x] 原子递增当前用户的 `quota`
    - [x] 更新订单状态为 `success`，写入 `actual_amount` 与 `quota_amount`
    - [x] 写入充值历史记录（`top_ups`）
  - [x] 事务提交，实时生效
- [x] **2.5 订单状态轮询接口 (`GET /api/dist/topup/crypto/status`)** *(已在 `controller/crypto.go` 完成；已补充当前用户订单归属校验，阻止跨用户读取订单状态、TxHash 与到账信息)*
  - [x] 接收 `trade_no`，返回当前订单的最新状态（`pending` / `processing` / `success` / `failed` / `expired`）以及实际到账数额 `actual_amount`
  - [x] 适配前端轮询，检测到 `success` 立即弹窗提示并刷新前端余额
- [x] **2.6 前端交互适配 (`Topup.jsx` 扩展提交 TxHash)** *(已在 `api-route-deploy-new` 完成并构建成功)*
  - [x] 在前端加密货币支付弹窗中，增加“已完成转账？粘贴交易哈希（TxHash）”输入框与“提交哈希立即验证”按钮
  - [x] 支持用户在转账后粘贴哈希立即提交核验，极速到账
  - [x] 支持 Arbitrum One、TRON 网络选择与中英文国际化文本补齐
  - [x] 网络与币种选择移入弹窗；创建订单后显示本地生成的二维码和 10 分钟倒计时，随后隐藏地址并仅保留 5 分钟 TxHash 提交入口（前端 `e05b679`）

---

### 模块三：SubRouter 接口协议兼容层 (`/api/dist/*` 路由群开发)
- [x] **3.1 兼容路由组骨架注册 (`router/dist-router.go`)** *(已完成并挂载至主路由)*
  - [x] 挂载 `/api/dist` 顶级路由组，注入标准鉴权中间件与请求头解析（支持 `New-Api-User` 标头与 Session Cookie 互通）
- [x] **3.2 站点公开信息与定价接口** *(已在 `controller/dist.go` 完成)*
  - [x] `GET /api/dist/site/info`：返回站点名称、模板主题、启用支付方式列表（包含 `crypto`）、货币符号与汇率
  - [x] `GET /api/dist/site/models`：动态输出当前系统启用的可用模型列表
  - [x] `GET /api/dist/site/pricing`：返回各个模型的定价详情与分组倍率
  - [x] `GET /api/dist/site/packages`：返回在售套餐包列表
  - [x] `GET /api/dist/topup/info`：返回支持的支付方式、加密货币钱包配置（Arb/Tron 钱包地址）、最小充值额度等
  - [x] `POST /api/dist/topup/amount`：根据汇率动态计算支付金额与到账额度
  - [x] 支付宝改用 USD/CNY 官方日汇率，每 24 小时更新；失败时沿用最近成功值，冷启动使用原配置兜底；Stripe 汇率由 Stripe 处理，并删除前端多币种请求与缓存（后端 `7d025b864`，前端 `0db6152`）
- [x] **3.3 用户体系接口兼容 (`/api/dist/user/*`)** *(已在 `controller/dist.go` 完成)*
  - [x] `POST /api/dist/user/register`：用户注册接口适配
  - [x] `POST /api/dist/user/login`：用户登录接口适配（包含双轨账号探测）
  - [x] 分发前端适配 Access Token + HttpOnly Refresh Cookie 会话协议，修复登录成功后 `/user/self` 立即 401；已覆盖登录、刷新恢复、退出及初始化页返回地址回归
  - [x] `POST /api/dist/user/logout`：退出登录
  - [x] `GET /api/dist/user/self`：获取当前登录用户信息与 Quota 余额
  - [x] `PUT /api/dist/user/password`：修改密码接口适配
  - [x] `GET /api/dist/user/usage`：用户调用量折线图统计
  - [x] `GET /api/dist/user/logs` & `/api/dist/user/logs/stat`：模型调用明细日志与聚合统计
- [x] **3.4 API Key 令牌管理接口 (`/api/dist/token/*`)** *(已在 `controller/dist.go` 完成)*
  - [x] `GET /api/dist/token/list`：分页查询用户的 API Keys（直接返回数组兼容前端）
  - [x] `POST /api/dist/token/create`：创建新的 API Key（生成 `sk-...` 令牌）
  - [x] `PUT /api/dist/token/:id`：修改 Key 名称、过期时间、配额限制
  - [x] `DELETE /api/dist/token/:id`：删除或作废 Key
  - [x] `GET /api/dist/token/:id/models`：查询该 Key 允许访问的模型列表
- [x] **3.5 传统支付与兑换码接口打通** *(已在 `controller/dist.go` 完成)*
  - [x] Creem 支付明确不在本项目范围；后端保持 `enable_creem_topup: false`，不实现 `/api/dist/topup/creem/pay`
  - [x] `POST /api/dist/topup/redeem`：卡密/兑换码兑换额度接口
  - [x] `POST /api/dist/topup/pay`：易支付（微信/支付宝）下单对接
  - [x] `POST /api/dist/topup/stripe/pay`：Stripe 信用卡支付对接
  - [x] 分站兼容接口按 Stripe API/Webhook/Price 与合规配置动态启用 Stripe，最低充值默认值调整为 $5
  - [x] `GET /api/dist/topup/history`：充值记录明细查询
- [x] **3.6 套餐购买与订阅结算 (`/api/dist/package/*`)** *(已在 `controller/dist.go` 完成)*
  - [x] `POST /api/dist/package/subscribe`：套餐扣减余额购买与激活逻辑
  - [x] `GET /api/dist/package/subscriptions`：查询用户当前生效中的套餐包
- [ ] **3.7 邀请充值返佣闭环**
  - [x] 服务端按邀请人数阶梯计算返佣比例（默认 5%，20/50/100 人对应 7%/10%/15%）
  - [x] 被邀请用户真实充值成功后原子写入唯一返佣流水并增加邀请人返佣额度，重复回调不得重复发放
  - [x] `/api/dist/aff` 与 `/api/dist/aff_earnings` 返回真实比例、余额和分页流水，并与分发前端对齐
  - [ ] 覆盖注册绑定、充值返佣、阶梯比例、无邀请人、重复结算和并发安全回归：SQLite 定向及模型全量测试通过，升级迁移与重复启动通过；MySQL 5.7.44 / PostgreSQL 9.6.24 实库用例已加入 CI，待实际运行验证

---

### 模块四：双轨智能网关分流（零垫资旧站流量接管）
- [x] **4.1 网关 Key 探测与分流中间件** *(已在 `middleware/dist_gateway.go` + `service/dist_proxy.go` 完成并通过测试；2026-09-16 修复为旧 Key 只有在 SubRouter 明确额度耗尽后才切换本地，并持久化 exhausted 状态)*
  - [x] 在 `/v1/*` 核心中转路由前置检查 `Authorization: Bearer <key>`、`x-api-key`、`mj-api-secret` 等
  - [x] 校验逻辑：
    - 若 `<key>` 存在于本地且对应账户可用（已充值有配额） -> 执行本地 New API 渠道中转扣费
    - 若 `<key>` 在本地未查到（判定为旧站老用户 Key） -> 执行反向代理透传至 `https://apiroute.subrouter.ai/v1/*`
    - 若 `<key>` 属于迁移导入的老 Key 且新站额度为 0 -> 优先透传 SubRouter 扣除旧站残留余额，零垫资
  - [x] 透传时保留原始 HTTP Header、流式（SSE）长连接与超时配置 (`FlushInterval = -1`, `ResponseHeaderTimeout = 300s`)
- [x] **4.2 旧额度耗尽与充值引导** *(已在 `service/dist_proxy.go` 完成并通过测试；2026-09-16 增加上游额度耗尽标记回传，避免本地余额提前垫付)*
  - [x] 捕获 SubRouter 返回的 `429 Insufficient Quota` 及额度不足错误
  - [x] 格式化向前端输出友好提示，引导用户前往 `https://www.api-route.com/topup` 充值
- [x] **4.3 用户首次登录自迁移（密码与历史 Key 抓取）** *(已在 `service/dist_migration.go` + `controller/dist.go` 完成并通过测试)*
  - [x] 收紧回源认证：仅允许 `Setting.subrouter_id` 已标记、账户启用且本地密码为空的旧站账户回源；密码与历史 Key 在同一事务中迁移，完成后永久禁止再次回源覆盖；SQLite 回归已覆盖未标记、已迁移、重复迁移、Session Cookie 携带与 Key 抓取失败回滚。
  - [ ] 本机没有 MySQL 5.7.8+ / PostgreSQL 9.6+ 实例，以上事务路径尚未完成两种数据库的实库回归，不能据此宣称三数据库兼容验证完成。
  - [x] 用户登录时若本地未录入密码，先代理向上游 SubRouter 验证凭据
  - [x] 验证成功自动将密码加密存入本地库（严格设置初始 Quota = 0，恪守零垫资原则）
  - [x] 利用已认证 Session 自动抓取导入该用户的历史 API Key 列表，并打标 `Group = "subrouter"`
- [x] **4.4 SubRouter 历史存量客户全量同步工具与数据库写入** *(已完成)*
  - [x] 逆向/解析 SubRouter 分销商管理鉴权机制（Session Cookie + `New-Api-User: 4870` 请求头）
  - [x] 开发 `cmd/sync_customers` CLI 批量同步与入库工具，支持在线拉取与本地快照载入
  - [x] 成功抓取全部 520 位存量客户数据，生成完整数据快照 `subrouter_customers_backup.json`
  - [x] 全量 520 位客户写入本地数据库 `users` 表，实现 0 失败率：
    - [x] 针对同名/冲突用户名自动按客户 ID 进行唯一化重命名（如 `poc_20093`）
    - [x] 批量生成全局唯一 `aff_code`，规避数据库唯一索引约束冲突
    - [x] 严格落实“零垫资”防线（新站 Quota 一律设为 0，历史配额与使用量存证于 `Setting` 与 `Remark`）
    - [x] 单元测试 `TestSyncCustomersFromRecords` 100% 覆盖并验证写入与更新幂等性

---

### 模块五：全链路联调验证与生产割接 (Verification & Cutover)
- [ ] **5.1 本地联调测试**
  - [x] 已完成后端兼容路由编译检查、核心迁移/网关/链上验证定向测试、`api-route-deploy-new` 生产构建，以及本地代理、站点信息、注册、登录、用户信息和 API Key 创建联调。
  - [x] 已配置临时 SQLite 渠道并使用运行时密钥完成真实 `/v1/chat/completions` 非流式与流式测试；前端代理转发、模型响应和本地额度扣减均正常。
  - [x] 未修改只读生产前端；`api-route-deploy-new/vite.config.js` 的本地代理已指向 `http://127.0.0.1:3000` 验证。
  - [x] 可编辑前端已接入中文管理员工作区（用户、额度、渠道、模型上下架与倍率、充值补单、兑换码、日志、Root 设置），并修正模型状态更新接口与中文统计标签。
  - [x] 按生产运维优先级补齐可编辑前端管理能力：
    - [x] 高级渠道管理：已接入编辑、连通测试、余额查询、上游模型拉取、模型映射、多密钥、优先级/权重、代理与自动禁用；前端生产构建通过（2026-09-18）。
    - [x] 完整模型管理：已接入模型元数据搜索与增删改、模型上下架、供应商增删改、渠道绑定/端点展示，以及 io.net 配置状态、连接测试和现有部署列表；会产生外部费用且当前未配置的 GPU 新建/续期操作不在割接必需范围（2026-09-18）。
    - [x] 完整日志与系统健康状态：已接入日志类型、用户、模型、渠道、请求 ID 与时间范围筛选，消费统计、审计日志、公开服务状态，以及 Root 可见的实例和系统任务；已下架的前端任务中心不重新接入（2026-09-18）。
    - [x] 订阅套餐与用户订阅管理：已接入套餐增改、启停，按用户查询与分配订阅、重置额度和立即失效；不暴露高风险的硬删除入口（2026-09-18）。
    - [x] 分类系统设置：已按认证与注册、支付状态、SMTP、限流、监控和站点内容分类接入；密钥、令牌、钱包地址等敏感配置不在前端展示（2026-09-18）。
  - [ ] 继续补齐非核心管理能力：
    - [x] 用户安全管理：已接入用户详情/删除、登录绑定清理、Passkey 重置、2FA 统计与管理员关闭 2FA；高风险操作使用一次性证明绑定具体用户与具体动作，完成后撤销目标用户会话、发送安全通知并记录不含凭据的审计信息。依据 OWASP ASVS 5.0.0 V6.4.6、V7.5.3、V8.4.2、V16.3.1 及 Authentication/MFA Cheat Sheets；定向后端回归与前端生产构建通过（2026-09-18）。
    - [x] 渠道批量运维：已接入本页多选、批量启停/删除/设置标签、全量测试与余额刷新、渠道复制，并修正单渠道“手动禁用”状态值；前端生产构建及四语言文案检查通过（2026-09-18）。
    - [x] 模型与供应商批量维护：已接入缺失模型检查、官方元数据同步预览/应用、模型批量删除，以及供应商批量分配/合并/删除的预览与应用；所有批量供应商变更均强制先预览并携带版本号应用。前端生产构建、四语言文案检查及元数据同步定向测试通过；供应商/删除数据库矩阵完成业务断言后仍因既有 Windows `audit.db` 句柄清理失败而返回失败（2026-09-18）。
    - [x] 兑换码批量维护：已接入名称/ID 与状态搜索、本页多选、批量删除和清理已使用/禁用/过期兑换码；前端生产构建、四语言文案检查及后端搜索/清理/批量删除定向测试通过（2026-09-18）。
    - [x] 套餐全局额度重置：已接入按套餐重置全部生效订阅额度，并推进下一重置周期；操作前明确确认，结果显示订阅数和用户数。前端生产构建、四语言文案检查及后端套餐批量重置定向测试通过（2026-09-18）。
    - [x] 系统运行维护：已在 Root 管理页面接入内存、协程、GC、磁盘缓存和磁盘占用统计，以及磁盘缓存清理、统计重置与手动 GC；变更操作均有二次确认，前端生产构建与四语言文案检查通过（2026-09-19）。
    - [x] 服务日志文件维护：已接入日志文件数量、大小、时间和最近文件列表，并支持按保留数量或保留天数清理历史文件；当前活跃日志由后端保护，前端生产构建与四语言文案检查通过（2026-09-19）。
    - [x] 上游倍率同步：已接入官方预设、models.dev 和现有渠道的来源选择与端点配置，支持差异预览、连通结果展示，并按模型选择来源后携带价格版本批量应用；前端生产构建、四语言文案检查及后端倍率同步定向测试通过（2026-09-19）。
    - [x] 邮箱验证与绑定兼容：普通账号注册强制邮箱验证码，第三方 OAuth 注册不追加邮箱绑定要求；复用 Resend 邮件发送，并接通生产前端 `/api/dist/user/email/*` 初次绑定接口。兼容流程仅允许无邮箱账户、有效浏览器会话且最近 10 分钟内重新登录的用户，邮箱替换继续使用安全证明流程；过期、重放、错误验证码及非会话/过期登录绕过定向回归通过。依据 OWASP ASVS 5.0.0 V2/V6.4/V7.5、Email Validation and Verification、Authentication 与 Session Management Cheat Sheets（2026-09-19）。
    - [x] API Key 管理：已接入密钥列表、敏感值确认显示与批量删除。
    - [x] 详细数据与流量分析：已并行接入总用量、按用户聚合与流量明细查询。
    - [x] 自定义 OAuth 提供商管理：已接入 OIDC 自动发现、完整配置增改与删除。
    - [x] 预填分组管理：已接入 model/tag/endpoint 分组及条目增改删。
    - [x] 异步任务与 Midjourney 管理记录：已接入筛选与完整记录查看。
    - [x] 高级系统运维：已接入系统任务当前状态/详情、失联实例清理与渠道亲和缓存查看/清空。
    - [x] GPU 部署完整生命周期：已接入硬件/地区/部署列表、名称与副本检查、价格估算、创建、更新、续期、日志/容器详情与删除。
    - [x] 高级渠道运维：已接入 Codex、Ollama、vLLM、SGLang 与上游模型更新检测/应用。
    - [x] 高级价格工具：已接入价格预览、转换与模型倍率重置。
    - [x] 任务插件管理：已接入运行时状态、上传、启停、版本、激活、试运行、版本删除与市场来源维护；以上 10 组统一进入 Root 可见“高级管理”页，四语言文案与前端生产构建通过（前端提交 `cfb7d9a`，2026-09-19）。
  - [x] 2026-09-17 修复管理员充值搜索使用旧页码的问题：输入值与已应用关键词分离，提交后回到第一页；前端生产构建通过。浏览器工具返回 unsupported Codex auth method: apikey，交互与视觉验证仍待完成。
  - [x] 2026-09-20 修复后端四包测试隔离：渠道亲和缓存键改用测试名避免 Windows 时钟碰撞；SubRouter 测试恢复全局数据库；审计 SQLite 与重复初始化连接均在清理前关闭；OAuth 与 Kling 用例对齐当前流程和 `Reserve` 目标额度语义。`go test ./middleware ./service ./controller ./model -count=1` 连续两轮通过。
  - [x] 2026-09-20 管理员充值记录补充充值用户身份：列表接口联表返回用户名与邮箱，前端同时展示用户名、邮箱和用户 ID；列表与订单搜索回归、controller 编译、前端生产构建通过，并在 PostgreSQL 15.19 对生产数据完成只读联表验证。
  - [x] 2026-09-20 完成 PostgreSQL 15.19 上线演练：生产镜像在隔离库完成全新迁移、当前生产 schema 升级及重复迁移，规范化 DDL 哈希保持一致；自定义格式备份恢复后 schema 哈希和哨兵数据一致；Token/预填分组/约束/返佣/会话字段、兑换批量删除、即时任务结算、模型管理、模型定价和固定价格计费实库回归通过。零垫资数据形态在事务内验证后回滚；全部临时库、角色、备份及隧道已清理，生产用户数仍为 2，三个容器保持 healthy。
  - [x] 2026-09-20 新增只读一键切流前检查 `scripts/cutover-preflight.sh`：检查 Compose、三容器健康、核心环境变量存在性、Session Secret、Secure Cookie/可信 Origin、本地状态与 SubRouter 分发入口、PG15 版本与用户数、磁盘、24 小时内数据库备份、后端镜像和 Nginx 配置；不输出密钥值，任一硬门槛失败即返回非零。VPS 已配置 Secure Cookie 与 `https://www.api-route.com` 可信 Origin；切流前生成 `/data/api-route/backups/pre-cutover-20260920-111640.dump` 并恢复至隔离库核对用户、Token、渠道和充值记录，最终预检为 16 项通过、0 失败、0 警告。
  - [x] 2026-09-20 完成回滚演练：新增 `scripts/cutover-rollback.sh` 与 `docs/cutover-runbook.md`，部署工作流会在构建前保留当前健康镜像并在部署或最终健康检查失败时自动恢复；VPS 已用同一镜像真实重建后端容器，用户数保持 2、三个 OAuth 配置仍加载、容器恢复 healthy，数据库和余额未变。
  - [x] 2026-09-20 新增 `scripts/cutover-observe.sh`，固定输出 API 状态、三容器健康/重启、5xx/429/超时、SubRouter 代理/额度耗尽/登录迁移、消费/错误/充值/退款、PostgreSQL 连接和 Redis 指标；VPS 五分钟窗口实测通过。
  - [x] 验证页面渲染：首页、67 个模型的模型广场、登录、注册和文档均完成桌面/手机、亮色/暗色及深层路由浏览器回归，无横向溢出或浏览器错误；前端生产构建和全部项目检查通过，并修复已下线分站 Tour 对应的过期检查（2026-09-20）。
  - [x] 2026-09-20 确定支付首发策略：未经真实端到端验证的支付方式保持关闭；加密充值默认关闭并与全局支付合规开关联动，显式启用后才允许创建订单或提交交易哈希；Stripe webhook 日志不再记录完整签名和请求体。真实支付仍须在正式切流前由人工完成小额验收。
  - [x] 2026-09-20 修复加密充值待支付订单恢复：新增当前用户加密订单历史接口，充值记录合并展示待支付/处理中订单，关闭或刷新支付窗口后可从“继续支付”恢复地址、倒计时与交易哈希提交；过期订单不可恢复。Go controller/model 回归、前端支付检查与生产构建通过。
  - [x] 2026-09-21 修复分档 Token 模型价格展示：价格页与模型详情页按表达式首个标准档展示，不再错误取长上下文最高档；GPT/Grok 条件价格解析回归与前端生产构建通过。
  - [x] 2026-09-21 增加站点公告铃铛：顶部语言按钮左侧展示通知入口，读取 Root 后台“系统公告（Notice）”配置并支持空状态、数量角标、点击外部与 Esc 关闭；公开接口回归、四语言检查、前端生产构建及本地页面验证通过。
  - [x] 2026-09-21 优化新建 API Key 的分组下拉：直接复用模型广场的供应商 Logo 解析，Standard 分组文字使用绿色、Enterprise 分组文字使用蓝色；下拉保留内部滚动条并扩大可视高度，支持后续大量分组；前端生产构建通过。
  - [x] 验证用户流程：注册、登录、获取用户信息、创建 API Key。
  - [x] 验证充值流程：已完成真实加密货币充值测试，订单创建、转账哈希提交、链上核验及自动充值入账正常（2026-09-20）。
  - [x] 验证 API 调用：使用本地新 Key 请求 `/v1/chat/completions` 非流式和流式打字机测试。
  - [x] 验证零垫资路由边界：未知旧 Key 转发 SubRouter，收到上游 401 时本地用户额度未变化；网关定向回归测试通过。2026-09-20 使用仍有旧站余额的真实旧 Key 经 VPS 新后端请求 `/v1/chat/completions`，`glm-5.3-flash` 返回 200（14 个输入 Token、3 个输出 Token）；运行日志明确记录 `SubRouter Proxy`，本地消费日志总数保持 3 条不变，确认请求未进入新站渠道选择或本地扣费。
  - [x] 2026-09-17 修复本地检查入口：`api-route-deploy-new` 新增中文 `/setup` 初始化页及 `/api/setup` 调用，避免误打开后端 `3000` 端口的占位 `web/dist` 页面；前端构建通过。
  - [x] 提交前核心定向测试通过；2026-09-20 已修复 Windows/SQLite 临时库连接清理与相关测试状态污染，四包全量测试连续两轮通过。
- [ ] **5.2 VPS 生产环境构建与部署**
  - [x] 修复生图与视频模型计价：每天从 `apiroute.subrouter.ai` 原样同步按次/按秒售价并统一加价 5%，媒体模型不再套用官方 Token 输入输出价；生产已确认 Nano Banana / GPT Image 为按次价、Seedance 为按秒分辨率表达式，SubRouter 渠道倍率固定为 1 防止重复加价（后端 `2fa4f79b5`，前端 `f659d1a`，2026-09-19）
  - [x] 提交代码并由 GitHub Actions 自动构建部署至 VPS（提交 `686dce6df`，Deploy API Route #6 成功，耗时 3m56s）
  - [x] 通过部署密钥注入 Stripe、ZPay、GitHub、Google、X 配置，后端不得持久化或输出明文密钥（运行时文件注入与生产读取已验证）
    - [x] 完成环境变量覆盖、Compose 透传、GitHub Actions 经 SSH 加密传输的临时编码文件注入和容器 `/data/.env.runtime` 读取代码；敏感值不进入 OptionMap、数据库或仓库
    - [x] 已从忽略文件安全写入 11 项仓库 Actions Secrets；密钥值未进入 diff、日志或提交
    - [x] OAuth 元数据启动逻辑通过 SQLite、MySQL 5.7.44、PostgreSQL 9.6.24 实库兼容与重复执行测试（CI run `35172661409` 的定向步骤成功）
    - [x] 推送部署后验证容器实际读取：Deploy run `35174371207` 成功；`/api/dist/site/info` 返回 `oauth_origin=https://www.api-route.com`，并发现 Google/GitHub/X 三个提供商
  - [ ] 接通并回归 `/api/dist/oauth/{google,github,x}` 登录兼容接口与 Stripe/ZPay 支付回调
    - [x] 完成分销站 OAuth start/callback、PKCE S256、10 分钟服务端 state、支付方式发现及定向回归测试
    - [ ] 在部署环境完成三方真实回调与支付沙盒/小额实测；本机对 Stripe/ZPay 的 TLS 握手失败，未将网络失败误判为凭据失败
  - [x] VPS Nginx 已能将 `/api/dist/*` 转发到 Go 后端容器；部署后 `/api/dist/site/info` 已实际访问验证
  - [x] 链上查询基础配置已就绪：Arbitrum 使用公共 RPC，TronGrid 可直接按交易哈希查询且 API Key 仅为可选的限流/稳定性增强；Arb/Tron 平台收款钱包已配置并由前端正常展示。真实小额交易与自动到账仍按上方“验证充值流程”待办验收。
- [x] **5.3 前端生产切流割接**（2026-09-20 已完成）
  - [x] **正式割接硬门槛：切流前最后一次从旧站拉取完整用户清单，生成旧站用户迁移白名单快照，并核对总数、旧站 ID/用户名/邮箱唯一性及字段完整性；不得提前写入正式 `users` 表，用户首次登录验证成功后再按需创建本地账户。** 2026-09-20 19:12（UTC+8）最终拉取 537 条，旧站 ID 无空值/重复、用户名无空值并保留 1 个历史重复值、467 个非空邮箱无重复；快照 SHA-256 为 `3D33766FDD6514E7705E9F5C32810B756C7B5C0DD86EFBAAA1EF67858397E134`，未写入生产数据库。
  - [x] **正式割接硬门槛：新后端至少配置一个真实可用的生产上游渠道，完成凭据、模型映射、定价与额度策略配置，并使用本地新 Key 通过非流式、流式真实调用及扣费核对；同时验证上游不可用时不会错误扣费或回退到旧用户余额。此项未完成不得触发生产切流。** 2026-09-20 使用临时本地 Key 经 `origin.api-route.com` 调用 `glm-5.3-flash`，非流式和流式均返回 200、各新增一条渠道 1 的消费日志并各扣 2 个配额单位；不存在模型返回 `model_not_found` 时额度与日志均不变，SubRouter 代理事件为 0；临时 Token 已删除。
  - [x] 在不改变 `www.api-route.com` 与 `global.api-route.com` 的前提下，为新后端准备独立源站域名与 SSL（`origin.api-route.com` 已解析至 VPS，Let's Encrypt 证书签发并验证 HTTPS 200，自动续期已启用，2026-09-20）
  - [x] 修改可编辑前端 `api-route-deploy-new/vercel.json` 的候选转发规则并验证回滚；管理 API、`global.api-route.com` 的 `/v1` 与 `/v1beta` 已切至 `origin.api-route.com`，前端生产构建通过并以提交 `1805139` 推送；切流前提交 `0b92aab` 可用于 Git 回滚（2026-09-20）
  - [x] 将 `apiroute.subrouter.ai` 配置为新后端的普通上游渠道，验证新后端用户、Key、计费与零垫资边界（真实旧 Key 经生产域名继续触发 `SubRouter Proxy`；临时本地 Key 的非流式、流式调用及扣费验证通过，失败请求不扣费且不回退旧站，2026-09-20）
    - [x] 渠道余额查询识别该分站返回的人民币余额，并按每日 USD/CNY 汇率换算为美元后保存和展示；定向回归测试通过（2026-09-19）
    - [x] 支持在单个渠道内按模型分配路由分组，并在渠道编辑界面维护 SubRouter 的模型分组（实现及 SQLite 定向回归完成；PostgreSQL 15.19 模型管理与定价矩阵实库验证通过，2026-09-20）
  - [x] 触发 Vercel 构建上线，观察生产流量与日志，完成整体业务平滑接管（官网与管理 API 返回 200，`global.api-route.com` 的 `/v1/models`、`/v1beta/models` 使用真实旧 Key 均返回 200，VPS 日志确认流量已进入新网关并继续代理 SubRouter；三个容器 healthy、重启数为 0，2026-09-20）
  - [x] 2026-09-20 修复 Vercel `trailingSlash: false` 与后端管理集合根路由末尾斜杠不兼容：为用户、渠道、模型、供应商、部署、Token、数据、OAuth、预填分组、任务、兑换码、日志和设置 13 个根接口增加精确源站重写；前端生产构建通过，生产无会话探测均返回预期 401，0 个 404/重定向循环（前端提交 `7de02ee`）。
